apiVersion: v1
kind: Pod
metadata:
  name: kubia-manual-another
  labels:
    run: kubia
spec:
  containers:
    - image: luksa/kubia
      name: kubia
      ports:
        - containerPort: 8080
          protocol: TCP
